name: Deploy Architecture Generator
on:
  push:
    branches: [main]
    paths:
      - 'architecture-pipeline/web/**'
      - '.github/workflows/deploy-architecture-generator.yml'
  workflow_dispatch:
permissions:
  contents: read
  pages: write
  id-token: write
concurrency:
  group: pages
  cancel-in-progress: true
jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: architecture-pipeline/web
      - name: Deploy
        id: deployment
        uses: actions/deploy-pages@v4
